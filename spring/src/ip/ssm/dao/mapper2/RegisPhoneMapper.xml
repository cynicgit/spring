<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ip.ssm.dao.mapper2.RegisPhoneMapper">
	
	<select id="fingPhoneByImei2Imsi" parameterType="PhoneInfo" resultType="RegisPhone">
		select * from regis_phone where imei=#{imei} and imsi=#{imsi}
	</select>

</mapper>